# CST-305_ODE
# CPU Temperature Model

**Programmers:** Thi Nguyen, Komal Khan
**Course:** CST-305 - Differential Equations

Models CPU temperature using ODEs. Solves numerically with `scipy.integrate.odeint` and visualizes results.

📖 **See [EXPLANATION.md](EXPLANATION.md) for complete details**

---

## How to Run

### Prerequisites
- Python 3.7+
- pip

### Steps

1. Navigate to directory:
   ```bash
   cd P1
   ```

2. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

3. Run:
   ```bash
   python cpu_temperature_model.py
   ```

### Output
- Terminal output with analysis
- `cpu_temperature_results.png` (4-panel plot)
- Interactive plot window
